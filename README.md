# Dog-VS-Cat-Calssifier
Image classification model that identifies whether an input image contains a dog or a cat. Trained on labeled images using a CNN, the model processes and predicts the correct class with high accuracy.
